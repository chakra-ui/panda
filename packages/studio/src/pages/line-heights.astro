---
import { TokenDictionary } from '@pandacss/token-dictionary'
import { config } from 'virtual:panda'
import { FontTokens } from '../components/font-tokens'
import Layout from '../layouts/Layout.astro'
import Sidebar from '../layouts/Sidebar.astro'

const tokenDictionary = new TokenDictionary(config.theme!)
const tokens = Object.fromEntries(tokenDictionary.categoryMap)
import { customDocs } from '../utils/custom-docs'
---

<Layout title={customDocs.pageTitle}>
  <Sidebar>
    <FontTokens
      client:load
      fontTokens={tokens.lineHeights}
      token="lineHeight"
      largeText
      text={`${customDocs.title} design system lineHeight specifies the vertical distance between two lines of text. You can peveiew this visually here.`}
    />
  </Sidebar>
</Layout>
