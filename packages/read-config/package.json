{
  "name": "@css-panda/read-config",
  "version": "0.0.1",
  "description": "Find and load panda config",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "author": "Abraham Aremu <anubra266@gmail.com>",
  "scripts": {
    "build": "pnpm tsup src/ --dts",
    "dev": "pnpm build -- --watch"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "bundle-require": "^3.1.0",
    "@css-panda/logger": "workspace:*",
    "@esbuild-plugins/tsconfig-paths": "^0.0.4",
    "esbuild": "^0.15.5",
    "find-up": "^6.3.0"
  }
}
