import { UtilityConfig } from '@pandacss/types'

export const svg: UtilityConfig = {
  fill: {
    className: 'fill',
    values: 'colors',
  },
  stroke: {
    className: 'stroke',
    values: 'colors',
  },
}
