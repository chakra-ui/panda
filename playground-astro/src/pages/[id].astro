---
import Loader from '@/components/loader.astro'
import Layout from '../layouts/Layout.astro'
import { PlaygroundWithId } from '@/components/playground-with-id'

const { id } = Astro.params as { id: string }
---

<Layout>
  <Loader targetId='playground' />
  <PlaygroundWithId client:only='react' id={id} />
</Layout>
