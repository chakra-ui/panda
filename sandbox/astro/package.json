{
  "name": "sandbox-astro",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm codegen && astro dev",
    "start": "pnpm codegen && astro dev",
    "build": "pnpm codegen && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "codegen": "panda codegen",
    "cssgen": "panda cssgen"
  },
  "dependencies": {
    "@astrojs/solid-js": "5.1.3",
    "astro": "6.1.5",
    "solid-js": "1.9.10"
  },
  "devDependencies": {
    "@pandacss/dev": "workspace:*",
    "@pandacss/preset-base": "workspace:*",
    "@pandacss/preset-panda": "workspace:*"
  }
}
