{
  "name": "sandbox-nuxt",
  "private": true,
  "scripts": {
    "build": "pnpm codegen && nuxt build",
    "dev": "pnpm codegen && nuxt dev",
    "generate": "pnpm codegen && nuxt generate",
    "preview": "nuxt preview",
    "codegen": "panda codegen",
    "cssgen": "panda cssgen",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@pandacss/cli": "workspace:*",
    "@pandacss/preset-base": "workspace:*",
    "@pandacss/preset-panda": "workspace:*",
    "@pandacss/vite": "workspace:*",
    "nuxt": "4.2.1",
    "vue": "3.5.25"
  }
}
