---
'@pandacss/cli': patch
---

Scaffold the default presets in `panda init`.

New v2 projects include `@pandacss/preset-base` and `@pandacss/preset-panda` by default, so common utilities and
conditions work immediately. Use `--skip-presets` to scaffold a bare config instead.
