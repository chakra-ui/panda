---
'@pandacss/cli': patch
---

Scaffold the default presets in `panda init`.

New v2 projects now include `@pandacss/preset-base` and `@pandacss/preset-panda` by default, so common utilities and
conditions work immediately. Use `--no-install` to scaffold a bare config instead.
