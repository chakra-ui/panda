---
'@pandacss/cli': patch
---

Add a contrast checker to `panda studio`. Pick two color tokens to see the WCAG contrast ratio and AA/AAA pass badges, so you can check pairings without leaving the viewer.
