---
'@pandacss/compiler': minor
'@pandacss/compiler-wasm': minor
'@pandacss/compiler-shared': minor
---

Add optional `originalValue` to tokens in the design-system JSON spec so tooling can read values before reference
expansion or derivation.
