---
'@pandacss/cli': patch
---

Update `panda --help` for citty 0.2. Flags now show value hints (`--cwd=<dir>`), `--color` / `--no-color` are both
listed, and unknown-command errors drop the `[error]` prefix.
