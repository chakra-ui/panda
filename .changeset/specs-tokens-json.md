---
'@pandacss/compiler': minor
---

Generate `styled-system/specs/tokens.json` again — a flat snapshot of your design tokens grouped by category (`{ data: [{ type, values: [{ name, value }] }] }`). Import it to build your own token viewer, feed docs, or hand your design system to an LLM.
