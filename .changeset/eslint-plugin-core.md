---
'@pandacss/eslint-plugin': patch
---

Add the first Panda ESLint rules and a recommended flat config.

- extraction diagnostics, included files, token paths
- deprecated APIs, raw values that should use tokens
- shorthand/longhand conflicts, property naming, invalid nesting, debug calls

Same rules are available for oxlint via `@pandacss/eslint-plugin/oxlint`.
