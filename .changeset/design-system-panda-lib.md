---
'@pandacss/compiler-shared': minor
'@pandacss/compiler': minor
'@pandacss/config': minor
'@pandacss/cli': minor
---

Add `panda lib` to package a Panda design system. It scans your source, writes the manifest and preset files, syncs package exports, and supports watch mode. Token conflicts warn and your app value wins; stale build info re-extracts instead of failing the build.
