---
'@pandacss/cli': minor
---

Add `panda debug` for bug reports.

It writes platform info, the resolved config, per-file extraction data, and the project stylesheet under
`<outdir>/debug`. Use `--dry` to print to stdout or `--only-config` to dump only the resolved config.
