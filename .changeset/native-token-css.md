---
'@pandacss/compiler': minor
'@pandacss/compiler-wasm': minor
---

Emit native token CSS in the Rust stylesheet compiler and align the default `cssVarRoot` with JS output (`:where(:root, :host)`).
