---
'@pandacss/cli': minor
---

Add the default `panda` command. Running `panda` with no subcommand now runs codegen and cssgen in one build.

The command supports the common build flags, including `--outdir`, `--outfile`, `--splitting`, `--clean`, `--check`, and
`--watch`.
