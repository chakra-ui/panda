---
'@pandacss/preset-typography': minor
---

Add `@pandacss/preset-typography` for prose-styled Markdown and CMS HTML.

Opt in with `presets: [typographyPreset()]` to get a `prose` recipe (size variants) and semantic color tokens with
dark-mode defaults.
